name: Node CI

on: [push]

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
      - name: wine install
        run: |
          sudo apt update && sudo apt install -y wine
      - name: npm install, build
        run: |
          cd application
          npm install -g @quasar/cli yarn --force
          yarn install
          quasar build --mode electron --publish || quasar build --mode electron --publish
      - name: Archive production artifacts
        uses: actions/upload-artifact@v2
        with:
          name: dist-upload
          path: |
            application/dist