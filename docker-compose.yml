version: '2'
services:
    uashield:
      build: .
      restart: always
      environment:
        WORKERS: '256'
        USEPROXY: 'false'
      networks:
        - shield

networks:
  shield:
    driver: bridge
