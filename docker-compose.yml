version: '2'
services:
    uashield:
      restart: always
      container_name: uashield
      image: ghcr.io/opengs/uashield:go-experimental
      environment:
        ONLYOK: '256'
        USEPROXY: 'true'
        TIMEOUT: '5000'
        WORKERS: '1024'
      network_mode: host